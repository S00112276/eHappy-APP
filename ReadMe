To Run This App:

API:
cd into eHappy-APP 
nodemon server
// If nodemon server does not work run: npm install --save

Angular Project:
cd into eHappy-App
cd angular-source
ng serve
// If ng serve does not work run: npm install --save