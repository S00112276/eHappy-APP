To Run This App:

API:
cd into eHappy-APP 
nodemon server
// If nodemon server does not work run: npm install --save

Angular Project:
cd into eHappy-App
cd angular-source
ng serve
// If ng serve does not work run: npm install --save

For Stripe API:
Card number: 4242 4242 4242 4242
CVC: Any 3 digits
Epiry: Any date in future